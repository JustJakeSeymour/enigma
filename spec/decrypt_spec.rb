require './lib/key'
require './lib/offset'
require './lib/shift'
require './lib/enigma'
require './lib/decrypt'

RSpec.describe Decrypt do
  it 'can print written file name, key, and date codes after process' do

  end
  
  it 'will require a read filepath (must exist) and write filepath (does not need to exist)' do
    
  end

  it 'requires a key or date unless cracking is intended' do

  end

  it 'verifies the read file is .txt' do

  end
end